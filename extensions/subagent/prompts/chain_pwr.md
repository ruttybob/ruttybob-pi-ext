---
description: Scout → plan → worker chain — full implementation pipeline
argument-hint: "<task>"
---

Invoke `subagent` in **chain** mode:

1. `scout` — find all code relevant to: $@
2. `planner` — create an implementation plan from scout findings ({previous})
3. `worker` — implement the plan ({previous})
