# AGENTS.md — advisor

- Restores advisor state on `session_start`, strips advisor messages before each turn.
- Config lives at `~/.config/rpiv-advisor/advisor.json` — do not change the path.
- Tool name is `advisor` (preserved from rpiv-pi@7525a5d).
- Prompt template: `prompts/advisor-system.txt`.
