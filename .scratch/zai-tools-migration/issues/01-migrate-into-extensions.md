# Миграция pi-zai-tools в extensions/zai-tools

Status: done
Category: enhancement

## Описание

Перенести pi-zai-tools из отдельного git-репозитория (`pi-zai-tools/`) в стандартный стек расширений проекта (`extensions/zai-tools/`). Цель — единая кодовая база, общая тестовая инфраструктура, один `package.json`.

## Задачи

- [x] Скопировать `pi-zai-tools/extensions/zai-tools.ts` → `extensions/zai-tools/index.ts`, `pi-zai-tools/src/` → `extensions/zai-tools/src/`
- [x] Обновить импорты в `index.ts` ( `'../src/...'` → `'./src/...'` )
- [x] Добавить `@modelcontextprotocol/sdk` в `package.json` dependencies
- [x] Создать stub `tests/stubs/@modelcontextprotocol/sdk.ts` + алиас в vitest.config.ts и tsconfig.test.json
- [x] Перенести unit-тесты из `pi-zai-tools/test/` в `tests/zai-tools/`, обновить импорты
- [x] Live-тест (`live-zai.integration.test.ts`) — перенести с `describe.skip` при отсутствии `ZAI_API_KEY`
- [x] Убедиться что `npm test` проходит (286/287 зелёные; 1 pre-existing fail в pi-quota timeout)
- [x] Сохранить README как `extensions/zai-tools/README.md`
- [x] Удалить `pi-zai-tools/` после верификации

## Критерии приёмки

1. `npm test` — все тесты зелёные
2. `npx tsc --noEmit` — нет ошибок
3. `extensions/zai-tools/` содержит полный код, `pi-zai-tools/` удалён
4. Pi автоматически подхватывает расширение (оно в `./extensions`)

## Комментарии

> *This was generated by AI during triage.*

**Рекомендация triage:** `ready-for-agent`. Задача полностью механическая — перенос файлов, обновление путей, добавление stub. Не требует дизайн-решений. Все входные данные известны.

**Подводный камень:** stub для `@modelcontextprotocol/sdk`. Тесты pi-zai-tools мокают `McpCaller` на уровне сервисов (объект с `callTool`), а не на уровне SDK. Возможно, достаточно минимального stub'а, который просто экспортирует пустые классы. Смотреть `test/extension.test.ts` и `test/web-search-service.test.ts` как образец.
